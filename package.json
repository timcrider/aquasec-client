{
  "name": "@timcrider/aquasec-client",
  "version": "0.2.8",
  "engines": {
    "node": ">=18.0.0"
  },
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "node --test-reporter=spec --test-reporter=tap --test-reporter-destination=stdout --test-reporter-destination=test.tap --test tests/*.js"
  },
  "keywords": [
    "aqua",
    "aquasec",
    "security",
    "api",
    "client",
    "rest"
  ],
  "author": "Timothy M. Crider <timcrider@gmail.com>",
  "license": "MIT",
  "description": "Simple REST client for AquaSec API",
  "repository": {
    "type": "git",
    "url": "git+https://github.com:timcrider/aquasec-client.git"
  },
  "bugs": {
    "url": "https://github.com/timcrider/aquasec-client/issues"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "dotenv-expand": "^10.0.0"
  },
  "devDependencies": {
    "body-parser": "^1.20.2",
    "commander": "^10.0.1",
    "express": "^4.18.2",
    "octokit": "^2.0.14",
    "selfsigned": "^2.1.1"
  }
}
