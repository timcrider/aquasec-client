{
  "dependencies": {
    "dotenv": "^16.0.3",
    "dotenv-expand": "^10.0.0"
  },
  "name": "aquasec-client",
  "version": "0.0.1",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "node --test-reporter=spec --test-reporter=spec --test-reporter-destination=stdout --test-reporter-destination=test.log --test tests/*.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "selfsigned": "^2.1.1"
  }
}
